import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  icon: {
    position: "absolute",
    right: -13,
    bottom: 14,
    width: 25,
    height: 25,
  },
});
export default styles;
